- shell:
    - command: sudo -u $SUDO_USER yay -S --needed - < pkglist.txt
      description: Installing packages with YAY
      stdin: true
      stdout: true
      stderr: true

      #-
      #command: bash ./scripts/npm_installations.sh
      #description: Setting up npm packages

